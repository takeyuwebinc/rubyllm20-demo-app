<%# For a run whose scenario is no longer defined: its result kind is unknown. %>
<pre class="max-w-prose overflow-x-auto whitespace-pre-wrap break-words rounded-md bg-gray-100 p-4 text-xs text-gray-800"><%= JSON.pretty_generate(run.result) %></pre>
